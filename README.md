# Ronny Das Portfolio

This project is now a Next.js version of the portfolio.

## Scripts

- `npm run dev` starts the local development server.
- `npm run build` creates the production build.
- `npm run start` serves the production build.

